# =============================================================================
# FinApp Frontend -- Environment Variables
# Copia este archivo a .env.local y ajusta los valores.
# NUNCA commitees .env.local
# =============================================================================

# URL base del backend (sin trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
