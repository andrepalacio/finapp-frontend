services:
  frontend:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
    env_file: .env.local
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
